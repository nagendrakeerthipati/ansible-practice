- name: nginx install and started
  hosts: frontend
  become: true
  tasks:
  - name: installing nginx
    ansible.builtin.package:
    - name: nginx
      state: present

  - name: enabling nginx
    ansible.builtin.service:
    state: started
    enabled: true